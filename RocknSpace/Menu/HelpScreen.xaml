<UserControl x:Class="RocknSpace.Menu.HelpScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RocknSpace.Menu"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <ContentControl Style="{StaticResource MenuBackground}">
        <Grid>
            <Label HorizontalAlignment="Center" Content="rock'N'space" Style="{StaticResource MenuTitle}" />
            <Label HorizontalAlignment="Center" Content="hElp" Style="{StaticResource MenuLabel}" Margin="0,156,0,0" />
            <Button Style="{StaticResource MenuButton}" Content="back" Click="Back_Click" Margin="460,593,-231,-593"/>
        </Grid>
    </ContentControl>
</UserControl>